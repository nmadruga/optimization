/**
 * Function to read graph from binary file
 * @param filename name of file to open
 * @param size pointer to the size of the graph 
 * @return graph in a pointer to vector
*/
node* read_map_binary(char * filename, size_t* size);